import matplotlib
import matplotlib.pyplot as plt
import kNN
import numpy as np

# group,labels=kNN.file2matrx("datingTestSet2.txt")
# #创建一个图
# plt.figure()
# #设定图像中的一些参数(比如子图个数，子图排列状况)
# plt.subplot(111)
# #根据标签内的数字，给不同标签的点不同的颜色和尺寸
# plt.scatter(group[:,0],group[:,1],np.array(labels)*15,np.array(labels)*15)
# plt.show()
#kNN.datingClassTest()
